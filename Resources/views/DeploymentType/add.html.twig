{% extends "DellaertWebappDeploymentBundle::layout.html.twig" %}

{% block content %}
	{% if form is defined %}
		<h1>Add deployment type</h1>
		<form action="{{ path('DeploymentTypeAdd') }}" method="post" {{ form_enctype(form) }}>
			{{ form_widget(form) }}
			<input class="button save left" type="submit" value="Save" /><input class="button middle" type="reset" value="Clear" /><a href="{{ path('DeploymentTypeList') }}" class="button right delete">Cancel</a>
		</form>
	{% elseif entity is defined %}
		<script type="text/javascript">
		setTimeout(function() {
			  window.location.href = "{{ path('DeploymentTypeList') }}";
			}, 3000);
		</script>
		<h1>Deployment type added: {{ entity.name }}</h1>
		<p>You will be returned to the deployment type list in 3 seconds...</p>
	{% endif %}
{% endblock %}
